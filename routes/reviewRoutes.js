// import express from "express"
// import { getAllReviews, createReviewDoctor, createReviewTester } from "../Controllers/reviewController.js"
// import { authenticate , restrict} from "../auth/verifyToken.js"

// const router= express.Router({mergeParams:true})

// router.route('/').get(getAllReviews).post(authenticate, restrict(["patient"]), createReviewDoctor)
// router.route('/').get(getAllReviews).post(authenticate, restrict(["patient"]), createReviewTester)

// export default router